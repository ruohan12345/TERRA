# Configurations

This directory contains a small set of reviewed public YAML files. Dataset, mask, checkpoint, and output locations are external to the repository. The root `test_env.sh` selects the synthetic-input correctness configurations by default.
